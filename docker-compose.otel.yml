# OTel overlay — rebuilds with OTel support + adds Jaeger for trace visualization.
#
# Usage (with managed mode):
#   docker compose -f docker-compose.yml -f docker-compose.managed.yml -f docker-compose.otel.yml up
#
# Jaeger UI: http://localhost:16686

services:
  jaeger:
    image: jaegertracing/all-in-one:1.68
    ports:
      - "16686:16686"   # Jaeger UI
      - "4317:4317"     # OTLP gRPC
      - "4318:4318"     # OTLP HTTP
    environment:
      - COLLECTOR_OTLP_ENABLED=true
    restart: unless-stopped

  mtclaw:
    build:
      args:
        ENABLE_OTEL: "true"
    environment:
      - MTCLAW_TELEMETRY_ENABLED=true
      - MTCLAW_TELEMETRY_ENDPOINT=jaeger:4317
      - MTCLAW_TELEMETRY_PROTOCOL=grpc
      - MTCLAW_TELEMETRY_INSECURE=true
      - MTCLAW_TELEMETRY_SERVICE_NAME=mtclaw-gateway
    depends_on:
      jaeger:
        condition: service_started
